services:
  redis:
    image: redis:alpine
    container_name: atributo
    ports:
      - '6379:6379'
    volumes:
      - atributo_redis_data:/data
    restart: unless-stopped
volumes:
  atributo_redis_data:
